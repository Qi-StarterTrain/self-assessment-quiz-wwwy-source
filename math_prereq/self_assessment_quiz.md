# 数学前置自测（开课前必做）

> **目的**：判断你是否具备学习扩散模型所需的数学基础。
> **使用方式**：闭卷做题，做完后对照答案自查。
> **判定标准**：
> - 全部正确 → 直接进入正式课程
> - 答错 1–3 题 → 重点复习对应章节，可正常上课
> - 答错 4–7 题 → 强烈建议先用 1 周时间补完 `prob_review.md` 和 `calculus_review.md`
> - 答错 8 题以上 → 建议先补完整的概率论和深度学习数学课程
>
> **预计耗时**：60 分钟

---

## 第一部分：概率与统计（5 题）

### 题 1（高斯分布基础）

设 $X \sim \mathcal{N}(\mu, \sigma^2)$，$Y = aX + b$（$a, b$ 为常数）。

写出 $Y$ 的分布。

> 答：_____$X \sim \mathcal{N}(a\mu, a^2\sigma^2)$______________

---

### 题 2（重参数化）

我们想要从 $\mathcal{N}(\mu, \sigma^2)$ 中采样 $x$，但希望梯度可以反向传播到 $\mu$ 和 $\sigma$。

写出实现这一目的的采样表达式。

> 答：$x = $ ______\mu+\sigma______________

---

### 题 3（高斯之和）

设 $X_1 \sim \mathcal{N}(\mu_1, \sigma_1^2)$，$X_2 \sim \mathcal{N}(\mu_2, \sigma_2^2)$，且 $X_1 \perp X_2$。

写出 $X_1 + X_2$ 的分布。

> 答：____________________

---

### 题 4（KL 散度定义）

写出连续型分布 $p(x)$ 和 $q(x)$ 之间的 KL 散度定义。

KL 散度满足哪两条性质？

> 答：
> $D_{\mathrm{KL}}(p \| q) = $ ____________________
>
> 性质 1：____________________
> 性质 2：____________________

---

### 题 5（贝叶斯公式）

写出贝叶斯公式，并解释下列三项的物理含义：先验、似然、后验。

> 答：____________________

---

## 第二部分：微积分与线性代数（4 题）

### 题 6（链式法则）

设 $L = f(g(h(x)))$，$x \in \mathbb{R}$。

写出 $\frac{\partial L}{\partial x}$ 的表达式。

> 答：____________________

---

### 题 7（梯度与方向导数）

设 $f(x, y) = x^2 + 3xy + y^2$。

写出 $\nabla f$ 在点 $(1, 2)$ 处的值。

> 答：$\nabla f(1, 2) = $ ____________________

---

### 题 8（矩阵迹的导数）

设 $A \in \mathbb{R}^{n \times n}$，$x \in \mathbb{R}^n$。

下列等式是否成立？$\frac{\partial (x^\top A x)}{\partial x} = (A + A^\top) x$

> 答：是 / 否，简述理由：____________________

---

### 题 9（积分换元）

计算 $\int_{-\infty}^{+\infty} \frac{1}{\sqrt{2\pi}\sigma} e^{-\frac{(x-\mu)^2}{2\sigma^2}} \mathrm{d}x = ?$

> 答：____________________

---

## 第三部分：深度学习与 PyTorch（5 题）

### 题 10（梯度反向传播的方向）

下列描述哪个是正确的？

A. 反向传播按照前向计算图的顺序，逐层向后计算梯度
B. 反向传播按照前向计算图的逆顺序，逐层向后计算梯度
C. 反向传播只对叶子节点计算梯度
D. 反向传播需要存储所有中间激活

（多选）

> 答：____________________

---

### 题 11（PyTorch 张量操作）

下列代码的输出是什么？

```python
import torch
x = torch.arange(12).reshape(3, 4)
y = x.permute(1, 0)
print(y.shape)
print(y.is_contiguous())
```

> 答：
> `y.shape = ` ____________________
> `y.is_contiguous() = ` ____________________

---

### 题 12（Loss 函数）

下面这段代码计算的是什么 loss？写出它的数学表达式。

```python
loss = ((pred - target) ** 2).mean()
```

> 答：____________________

---

### 题 13（优化器）

Adam 优化器相比 SGD，主要的两个机制是什么？请简要说明。

> 答：
> 机制 1：____________________
> 机制 2：____________________

---

### 题 14（U-Net 结构）

简述 U-Net 的核心结构特征（用一段话回答）。

> 答：____________________

---

## 第四部分：综合应用（1 题）

### 题 15（综合）

设我们有一个简单的"加噪—去噪"过程：

- 给定干净图像 $x_0$
- 加噪：$x_1 = \sqrt{1-\beta} \cdot x_0 + \sqrt{\beta} \cdot \epsilon$，其中 $\epsilon \sim \mathcal{N}(0, I)$，$\beta \in (0, 1)$ 是常数

请回答：

(a) $x_1$ 的均值和方差是什么？

> 答：均值 = ____________________；方差 = ____________________

(b) 给定 $x_0$，$x_1$ 服从什么分布？

> 答：____________________

(c) 如果我们想从 $x_1$ 恢复 $x_0$，最朴素的做法是训练一个网络 $f_\theta(x_1)$ 预测 $x_0$，写出对应的 MSE 训练目标。

> 答：____________________

(d) 这个过程为什么是"扩散模型"思路的雏形？用 1–2 句话说明。

> 答：____________________

---

## 评分参考与下一步行动

完成后，请对照 `math_self_assessment_answers.md` 自查。

| 正确题数 | 建议行动 |
|----------|----------|
| 14–15 | 直接进入 W1 课程内容 |
| 11–13 | 复习答错部分对应的章节，1–2 天内补完 |
| 8–10 | 完整阅读 `prob_review.md` 与 `calculus_review.md`，预留 3–5 天 |
| ≤7 | 优先补完整的数学基础（推荐：MIT 18.650、3Blue1Brown 线代/微积分系列），再回来上这门课 |

---

## 附：本课程对数学的最低要求清单

如果你能熟练完成下列任务，你就具备这门课的数学基础：

- ✅ 看到高斯分布表达式，能立刻写出均值方差
- ✅ 知道 reparameterization 的形式与作用
- ✅ 能从 $p(x)$ 和 $q(x)$ 直接写出 KL 散度
- ✅ 能熟练运用链式法则做手动梯度推导
- ✅ 能读懂 PyTorch 中 `tensor.shape` 和 broadcasting 的行为
- ✅ 知道为什么需要梯度下降，以及 momentum 在做什么
- ✅ 见过或自己实现过一个 CNN/MLP 训练循环

如果其中有 ≥ 2 项不确定，强烈建议先补课。
